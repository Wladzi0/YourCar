{% extends 'base.html.twig' %}
{% block title %}
    {{ 'Your list of best cars'|trans }}
{% endblock %}
{% block body %}
    asdfas
    {# <div class=" container col-5 top d-flex flex-column"> #}
    {# <a href="{{ path('makes_list') }}" class="btn btn-success mt-4"> #}
    {# {{ 'Catalog of cars'|trans }} #}
    {# </a> #}
    {# <a href class="btn btn-success mt-4"> #}
    {# {{ 'Compating of cars'|trans }} #}
    {# </a> #}
    {# <a href class="btn btn-success mt-4"> #}
    {# {{ 'Your best cars'|trans }} #}
    {# </a> #}
    {# </div> #}
{% endblock %}
